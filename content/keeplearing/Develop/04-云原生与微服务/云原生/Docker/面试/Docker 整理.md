docker网络模式  
  
docker容器无法与宿主机某端口通信怎么排查

docker镜像和容器有什么区别？为什么要用docker？Linux namespace讲一下？Linux cgroup的层级结构讲一下？docker daemon是啥？发现docker daemon响应突然变慢了，如何排查？  
docker是什么（runtime+cli？）  
docker如何实现资源隔离  
写dockerfile能想到哪些注意点  
docker compose了解吗（不了解，但知道k8）   
k8 资源类型能说出哪些  
项目中使用k8的姿势  
k8s有哪些组件


#### k8s和docker的关系和区别
> Docker 解决的是“单个容器”的构建和运行问题，Kubernetes 解决的是“多个容器集群”的调度和管理问题。


#### dockerfile和docker-compose文件的编写：CMD和ENTRYPOINT的区别(这个我不太清楚)；
#### 怎么解决容器编排时容器间的依赖问题
#### 怎么查镜像
#### 讲讲DockerFile
#### 怎么下载其他分支 git branch相关
#### 单机docker支持那些网络模式
#### dockerfile 的结构
#### 部署成功 dockerfile 最少需要写那些东西，说出你认为必须的